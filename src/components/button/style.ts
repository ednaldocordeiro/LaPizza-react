import styled from "styled-components";

export const ButtonClick = styled.button`
  width: 15rem;
  height: 3rem;
  margin: 0 auto;
  border-radius: 2rem;
  background: var(--yellow);
  position: relative;
  top: -15px;
  cursor: pointer;
`